import { LogoAnimationDirective } from './logo-animation.directive';

describe('LogoAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
