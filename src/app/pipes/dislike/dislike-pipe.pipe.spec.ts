import { DislikePipePipe } from './dislike-pipe.pipe';

describe('DislikePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DislikePipePipe();
    expect(pipe).toBeTruthy();
  });
});
